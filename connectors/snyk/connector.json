{
  "$schema": "https://usepocket.net/schema/connector.json",
  "name": "snyk",
  "summary": "Scan dependencies and code for known vulnerabilities. Use before a release or when auditing a project.",
  "category": "Security",
  "author": "pocket",
  "transport": "stdio",
  "command": {
    "run": "npx",
    "args": [
      "-y",
      "snyk-mcp"
    ]
  },
  "auth": "apiKey",
  "scopes": [
    "read"
  ],
  "tools": [
    {
      "name": "test_dependencies",
      "description": "Scans a manifest for vulnerable dependencies and returns severities.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Project path. Defaults to the working directory."
          }
        },
        "required": []
      },
      "readOnly": true
    },
    {
      "name": "list_projects",
      "description": "Lists monitored projects and their issue counts.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "required": []
      },
      "readOnly": true
    }
  ],
  "license": "MIT",
  "homepage": "https://snyk.io"
}
